# Reference config for the Divine Jewels Netlify site.

[build]
  command = "npm run build:divine-jewels"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  VITE_TENANT = "divine-jewels"
  # Set VITE_API_URL in the Netlify UI
